## Shell tool preferences

- **Search**: always use `rg` (ripgrep) instead of `grep`
- **Find files**: always use `fd` instead of `find`
- **Directory navigation**: use plain `cd` (the builtin). Do NOT use `z`/zoxide — those are interactive shell functions and aren't available in non-interactive subshells.
